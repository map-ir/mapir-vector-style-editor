/// <reference types="react" />
import type { IProps } from 'types/general';
declare const MapirStyleEditor: ({
  map,
  locale,
  styleURL,
  sprite,
  title,
  columns,
  onSubmit,
  onCancle,
}: IProps) => JSX.Element;
export default MapirStyleEditor;
