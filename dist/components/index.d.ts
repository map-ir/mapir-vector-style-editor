/// <reference types="react" />
import type { IProps } from 'types/general';
declare const MapirStyleEditor: ({ map, locale, styleURL, sprite, title, columns, onSubmit, }: IProps) => JSX.Element;
export default MapirStyleEditor;
